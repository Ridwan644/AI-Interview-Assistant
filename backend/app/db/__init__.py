#I have to import the moddels (so Base knows about them) 
#create_all(engine) has to be called (so the tables get built) 

#imports your learn class into the models package's __init__ 
#from app.models.learn import Learn 

